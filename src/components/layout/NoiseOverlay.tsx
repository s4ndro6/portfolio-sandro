/* ============================================================
   AAZ Portfolio V4 — NoiseOverlay
   Fixed film grain overlay via SVG feTurbulence
   ============================================================ */

export default function NoiseOverlay() {
  return (
    <div
      className="noise-overlay"
      aria-hidden="true"
    />
  )
}
